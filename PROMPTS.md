
Each practice will consist of given number of phrases. Let user choose the length of training during setup. During practice, show progress bar. Only correct answers counts, fails does not count to the progress. After finishing the practice, show message and enable to start new practic.

==================

DONE: When a foreign language phrase contains blocks delimited by "pipe" (`|`), the blocks are possible alternatives. Any of these phrases is acceptable answer.

DONE: Enable the app to remember setup in config file so that user does not need to setup every time. Commit and push.

DONE: Create a git repository. Commit current state. Create new remote repository vocab-trainer and push.

DONE: Sounds fine. Now move current option "Edit Phrases..." to menu "Vocabulary" (it is already there). The button will be "Edit Phrase ..." and will edit current phrase - native language phrase will become editable and answer textfield will show correct answer and allow to edit it.

DONE: Hi! I need a simple Python app to help me practice language vocabulary. It will read phrases from Libre Office .ODS spreadsheet – each phrase is on one line. At the beginning, user selects working .ODS spreadsheet and specify column names for: 1) native`), 2) foreign language phrase (`foreign`), 3) numils`), 4) last attemptdate (`last`) and 5) total number of attempts (`attempts`). App reads the     vocabulary from given foldhen app shows nativephrase and asks user to put down foreign phrase. If the user fails increase   both number of fails and uand total number ofattempts. Otherwise decrease number of fails and update the rest as well. If  the number of attempts is ecreased. App will selectnext phrase randomly with respect to these rules: 1) phrases with fails have  priority but should not be 30 minutes, 2) app should ask each phrase approximately similar times – prefer phrases that were        tested before long time anh phrases, prefer thosewith lower number of total attempts. After user writes his answer and commits it by Enter or by a buttonnswer is right or wrongand shows user's response and correct answer. Enable editting both phrases and enable storing updated data to the original file.
